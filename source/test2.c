#include <stdio.h>

int main(void) {

    int x; 
    int y; 
    int sum;
    x = 100; 
    y = 200;
    sum = x + y; 
    printf("두수의 합 : %d", sum);
    return 0;
}

//%d 는 형식 지정자. 차례대로 변수를 지정하여 출력